name: Validate Code and Schemas

on:
  push:
    paths:
      - '3-AutomatedTesting/**'
  pull_request:
    paths:
      - '3-AutomatedTesting/**'

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install ajv-cli
        run: npm install -g ajv-cli

      - name: Validate JSON files
        run: |
          ajv validate -s 3-AutomatedTesting/3.1-Validation/test_samples/vote.schema.json -d "3-AutomatedTesting/3.1-Validation/test_samples/**/vote_*.json"

      - name: Install sqlfluff
        run: pip install sqlfluff

      - name: Lint SQL files
        run: |
          sqlfluff lint 3-AutomatedTesting/3.1-Validation/test_samples/**/*.sql
